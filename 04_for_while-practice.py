# для создания циклов
# for  
# while

# words = []
# for word in ['test', 'orange']:
#     words.append(word)
# print(words)

# numbers = [1, 2, 3]
# for number in range(4, 11):  # создаем переменную number, которая принимает новые значения из диапазона чисел от 4 до 11 (который моы создали с помощью range)
#     print('Номер из range: ', number)  # выводим на экран новое значение number
#     numbers.append(number)  # добавляем значение number в список
# print(numbers)

# numbers = [1, 2, 3]
# for number in numbers:
#     print('Начало итерации')
#     print(number)
#     print('конец итерации')
# print('Код вне цикла')



# n = input('Введите число: ')
# amount = 0
# for i in range(1, n):
#     amount += i
# print(amount)


'''
Хорошо, вот замена задачи:

---

### Задача 2: Таблица квадратов
Напиши программу, которая выводит числа от 1 до 10 и их квадраты.

**Пример вывода**:
```plaintext
1 в квадрате = 1
2 в квадрате = 4
3 в квадрате = 9
...
10 в квадрате = 100
```

**Подсказка**:
- Используй цикл `for` и операцию возведения в степень: `число ** 2`.

---

Если понадобится помощь или пояснения, обращайся! 😊
'''
# for n in range(1, 11):
#     print(n, 'в квадрате =', n ** 2)
#     print(f'{n} в квадрате = {n ** 2}')

# print('a' * 10)
# for i in range(1, 6):
#     print(' ' *  (5 - i) + '*' * i)

# Запрашиваем у пользователя высоту пирамиды
# height = int(input("Введите высоту пирамиды: "))

# Строим пирамиду
# for i in range(1, 10):
#     print(" " * (9 - i) + "*" * (2 * i - 1))


'''
### Средние задачи:

#### Задача 3: Проверка на простое число
Напиши программу, которая проверяет, является ли введённое пользователем число простым.  
Простое число — это число больше 1, которое делится только на 1 и на само себя.

Пример ввода и вывода:
Введите число: 7
Число 7 является простым.

Введите число: 10
Число 10 не является простым.
Подсказка: Используй цикл для проверки делимости числа на числа от 2 до (число - 1).

---
'''

n = int(input('Enter integer: '))
is_simple = True

for i in range(2, n):
    if n % i == 0:
        is_simple = False
    else:
        print(f'Простое число {i}')
    

if is_simple == True:
    print(f'Число {n} простое')
else:
    print(f'Число {n} не простое')

numbers = [1, 3, 7, 5, 2]  # Список чисел
есть_ли_четное = False  # Флажок: предполагаем, что чётных чисел нет

for num in numbers:
    if num % 2 != 0:  # Если нашли чётное число
        есть_ли_четное = True  # Устанавливаем флажок в True
        break  # Прерываем цикл, так как цель достигнута

if есть_ли_четное:
    print("В списке есть чётное число.")
else:
    print("В списке нет чётных чисел.")
